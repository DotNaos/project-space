---
title: Verification
description: Requirement and delivery evidence for owner-preserving Environment reconciliation.
---

Verification status: **Complete**

## Requirement evidence

- [x] ENV-OWN-001 — Owner-preserving built-in aliases
- [x] ENV-OWN-002 — Fail-closed duplicate behavior
- [x] ENV-ROUTE-001 — Evidence-based configured Host selection
- [x] ENV-OWN-003 — Repeatable and atomic ownership repair

## Final reconciliation

- [x] Owner provenance is retained through reconciliation and stripped from the public snapshot.
- [x] The exact cross-owner UUID collision has focused regression coverage.
- [x] Focused tests, type check, full tests, and build pass locally.
- [x] Migration `0059_tailscale_environment_ownership` remains historical and its runtime
  successor is repeatable and fail-closed: it copies
  only one current deployment observation with one exact user-owned connector identity/Host
  match and a completely equivalent built-in definition, leaves the deployment projection
  unchanged, and no-ops on stale, duplicate, wrong-owner, incompatible, user-defined, or
  two-connectors-to-one-Host evidence.
- [x] Combined presentation suppresses a deployment projection only for a same-UUID user built-in
  with resolved identity, manual user Host evidence, matching copied fields, and the exact
  `project-space:tailscale-deployment` owner; unresolved, unrelated, user-defined, and
  other-owner projected rows retain their evidence.
- [x] The configured start/read/send service path selects the exact verified Host and Environment and
  fails closed with an actionable typed unavailable result for missing, unresolved, or conflicting
  user-owned association evidence.
- [x] Deployment-only, absent, or duplicate same-UUID Host evidence remains generic and does not
  reveal cross-owner inventory.
- [x] `tests/codex-machine-tasks-persisted-ownership.integration.test.ts` begins with deployment-only
  state, invokes the real Tailscale refresh/classification boundary, and uses a stateful database
  proof that evaluates every ownership candidate condition before materializing the manual/user
  copied-blueprint row. It observes that row through the repository and exercises configured HTTP
  start, read, and send without a prebuilt inventory shortcut; stale ineligible evidence remains
  deployment-only and a deliberately incomplete query contract fails the proof.
- [x] The configured HTTP regression covers both exact Host selection and the authenticated
  `--here` equivalent while retaining strict authorization.
- [x] The CLI validates and preserves the typed unavailable result in JSON output.
- [x] PR #845 merged at `77b7c6c52c678fd31efe11daff6f8470d7a38296`; signed v0.27.5 and
  Production are healthy. Migration `0059` applied before the later fresh projection, leaving
  the exact candidate unrepaired.
- [x] Draft PR #846 carries this follow-up. Its corrected exact head, canonical preflight, and
  remote checks are recorded in the PR body and Manager handoff. No merge, release, deployment,
  or Production mutation is part of this follow-up.

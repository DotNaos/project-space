---
title: Tasks
description: Implementation and verification checklist for Environment ownership reconciliation.
---

## 1. Ownership boundary

- [x] 1.1 Carry database owner scope into the reconciliation records
  - **Evidence:** `server/database/repository.ts` selects and carries `owner_user_id` through the shared reconciliation pass, then removes only the temporary projection field.
- [x] 1.2 Scope aliases and references by owner
  - **Evidence:** `src/shared/compute-environment-api.ts` keys aliases by owner and definition ID and limits them to built-in records.
- [x] 1.3 Reconcile equivalent built-ins that share both UUID and blueprint across owners
  - **Evidence:** Scoped identity, rather than the bare UUID, determines whether a visible built-in
    is the canonical record or an owner-scoped alias.

## 2. Regression and compatibility

- [x] 2.1 Add the exact three-definition UUID collision regression
  - **Evidence:** Focused model and repository tests preserve the user-defined `x` record/reference while canonicalizing only the deployment built-in `x` to `a`.
- [x] 2.2 Preserve fail-closed duplicate and runtime authorization behavior
  - **Evidence:** The regression expects the duplicate-slug violation; the full test suite passes without changes to authorization boundaries.
- [x] 2.3 Audit persisted and projected paths
  - **Evidence:** Migration `0059_tailscale_environment_ownership` copies only one exact eligible
    deployment projection into the user scope; connector identity remains part of candidate
    uniqueness, so two connectors on one Host remain ambiguous. The user and deployment built-ins
    must match across every blueprint field. Stale, duplicate, wrong-owner, incompatible, and
    user-defined evidence remains untouched and fail-closed.
- [x] 2.4 Keep combined presentation owner-safe after the backfill
  - **Evidence:** The repository suppresses only the matching deployment projection when the
    user-owned copy has the complete repaired built-in signature and the projection belongs to
    the exact deployment owner, while retaining unresolved, user-defined, unrelated, and
    other-owner projected rows.
- [x] 2.5 Add the repeatable ownership reconciler
  - **Evidence:** `server/database/tailscale-environment-ownership-reconciler.ts` evaluates fresh deployment evidence globally and copies only one exact connector/Host candidate without changing migration `0059`.
- [x] 2.6 Bind reconciliation to evidence mutations
  - **Evidence:** Tailscale projection refresh, machine identity enrollment, connector inventory, and physical Host membership updates share a transaction-scoped advisory lock and reconcile before commit.
- [x] 2.7 Cover lifecycle and concurrency regressions
  - **Evidence:** `tests/tailscale-environment-ownership-reconciliation.test.ts` covers stale-to-fresh repair, post-startup Host membership, repeated no-op, ambiguity, and concurrent Host updates.

## 3. Delivery evidence

- [x] 3.1 Run focused tests, migration checks, type check, full tests, and build
  - **Evidence:** Focused lifecycle, repository, machine-connection, physical Host, and migration tests pass; `bun run check`, `bun test --isolate`, and `bun run build` pass.
- [x] 3.2 Verify the persisted configured repository/HTTP start/send flow
  - **Evidence:** `tests/codex-machine-tasks-persisted-ownership.integration.test.ts` starts with
    owner-scoped database query rows, reconciles the combined collision, lets the configured
    runtime construct its strict `ProjectSpaceDatabaseRepository` boundary over the same fixture,
    then performs HTTP start and send.
- [x] 3.3 Preserve private fail-closed Host diagnostics
  - **Evidence:** Missing, unresolved, and conflicting user-owned Environment association evidence
    returns a typed unavailable state and recovery message without a target or dispatch. An absent
    or duplicate Host, including deployment-only evidence, remains the generic unauthorized result.
- [x] 3.4 Preserve the typed result through the CLI
  - **Evidence:** The Go client validates the exact unavailable kind/state vocabulary and the
    `project codex start --format json` regression preserves it while returning a blocked outcome.
- [x] 3.5 Record the merged baseline and Production evidence
  - **Evidence:** PR #845 merged at `77b7c6c52c678fd31efe11daff6f8470d7a38296`; signed v0.27.5 and Production are healthy. Migration `0059` was applied after the sole eligible candidate's freshness expired, which is the repeatable-repair defect addressed here.

## 4. Deferred follow-ups

None.

---
title: Tasks
description: Implementation and verification checklist for the Codex Chat redesign.
---

## 1. Data boundaries

- [x] 1.1 Add an authenticated online-host inventory
  - **Evidence:** The endpoint combines user-owned machine membership with fresh deployment-owned Tailscale observations.
- [x] 1.2 Load sessions only for eligible online machine identifiers
  - **Evidence:** The page refreshes Tailscale first and passes only returned host identifiers to the Codex session controller.
- [x] 1.3 Keep local Codex access development-only
  - **Evidence:** The web server requires an explicit local machine and rejects that transport in production.

## 2. Interface

- [x] 2.1 Render the canonical Codex conversation in Chat
  - **Evidence:** The selected session uses the existing Codex task workspace and controller.
- [x] 2.2 Add grouped and collapsible task sections to the UI library
  - **Evidence:** The sidebar accepts host sections, defaults to four visible tasks, and supports controlled or local expansion.
- [x] 2.3 Keep machine, worktree, and disabled composer states visible
  - **Evidence:** HeroUI selects remain in the header and the empty Chat composer is disabled until selection.
- [x] 2.4 Support phone navigation
  - **Evidence:** A HeroUI drawer replaces the fixed sidebar below the desktop breakpoint.
- [x] 2.5 Bind issue task creation to its assigned worktree
  - **Evidence:** Task detail renders one fixed issue worktree, while general Chat keeps the host-scoped picker.

## 3. Verification

- [x] 3.1 Run focused Project Space and UI library tests
  - **Evidence:** Host, model, page, session, import, and grouped-sidebar tests pass.
- [x] 3.2 Type-check and build both repositories
  - **Evidence:** Project Space and the UI package type-check and build successfully.
- [x] 3.3 Dogfood desktop and phone layouts
  - **Evidence:** Desktop collapse and expansion, phone drawer, task selection, and horizontal overflow were visually checked.
- [x] 3.4 Verify live providers
  - **Evidence:** Deployment Tailscale OAuth returned fresh data and the local Codex app-server returned real tasks.
- [x] 3.5 Dogfood issue-bound and general task creation
  - **Evidence:** The signed-in live app shows only managed worktrees in Chat and the exact #763 worktree without a picker on task detail.

## 4. Deferred follow-ups

None.

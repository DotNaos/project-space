---
title: Reference
description: Exact generated and maintained contracts for commands, APIs, MCP tools, configuration, and output formats.
---

Reference pages state exact interfaces. They should be generated from code where practical;
curated guidance belongs in Getting Started, Development, or Operations.

## Command-line interface

The [CLI reference](/docs/cli) is generated from the real Cobra command tree and checked
for drift in CI.

## APIs and tools

- [HTTP API](/docs/api)
- [Project MCP](/docs/project-mcp)
- [Templates](/docs/templates)

## Configuration files

Common Project files:

```text
.project/template.lock.yaml
.project/template.values.yaml
deploy/deploy.yaml
```

## Output formats

Use `--format tsv` for agent-friendly command output where supported:

```sh
project validate --format tsv
project module list --format tsv
project template update --dry-run --format tsv
```

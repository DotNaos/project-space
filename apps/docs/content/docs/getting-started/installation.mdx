---
title: Installation
description: Install and update the Project CLI on supported systems.
---

The maintained installation and update instructions live on the existing
[Setup page](/docs/setup). That page remains the exact command source while this section
provides the stable entry point from the documentation structure.

After installation, use `project self-update` where supported and consult the
[generated CLI reference](/docs/cli) for the current contract.

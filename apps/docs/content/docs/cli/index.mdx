---
title: CLI
description: Generated command reference for the Project CLI.
---

{/* Generated by `bun run docs:cli:generate`. Do not edit this file by hand. */}

> **Documentation track:** current `main` source. This reference can be ahead of the latest published binary. Check `project --version` and the [latest GitHub release](https://github.com/DotNaos/project-space/releases/latest) before assuming a command is installed.

> Flag details cover managed macOS, Linux, and WSL installations. The native Windows CLI delegates runtime commands to WSL and can expose a smaller guidance-only flag surface.

This page is generated from the same Cobra command tree used by the `project` binary.

## Base Command

```sh
project
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for project |
| `-v`, `--version` | `bool` | local | version for project |

## Commands

| Command | Aliases | Description |
| --- | --- | --- |
| [`adopt`](#project-adopt) | - | Plan adoption of an existing project |
| [`agent`](#project-agent) | - | Manage the current Codex agent identity |
| [`chat`](#project-chat) | - | Coordinate with people and agents through Project Chat |
| [`codex`](#project-codex) | - | Start, inspect, continue, or attach to persistent Codex tasks |
| [`completion`](#project-completion) | - | Generate the autocompletion script for the specified shell |
| [`connect`](#project-connect) | - | Connect this machine to Project Space |
| [`connector`](#project-connector) | - | Retired compatibility command |
| [`control`](#project-control) | - | Run typed control-gateway operations |
| [`create`](#project-create) | `new` | Create and initialize a new project |
| [`deploy`](#project-deploy) | - | Deploy this project to the VPS |
| [`dev-build`](#project-dev-build) | - | Build short-lived development tools for an exact pull request head |
| [`disconnect`](#project-disconnect) | - | Disconnect this machine from Project Space |
| [`doctor`](#project-doctor) | - | Diagnose local or managed-machine Project Space readiness |
| [`environment`](#project-environment) | - | Discover Environment definitions and instances |
| [`help`](#project-help) | - | Help about any command |
| [`host`](#project-host) | - | Discover physical or virtual hosts |
| [`init`](#project-init) | - | Initialize a project template lock |
| [`inventory`](#project-inventory) | - | Discover the complete compute inventory |
| [`list`](#project-list) | - | List account projects and local availability |
| [`machine`](#project-machine) | - | Manage exact physical machines |
| [`module`](#project-module) | - | Manage project template modules |
| [`open`](#project-open) | - | Open a local project in the system's selected terminal |
| [`path`](#project-path) | - | Print a project's canonical local path |
| [`platform`](#project-platform) | - | Discover compute capacity providers |
| [`prepare`](#project-prepare) | - | Run trusted repository setup steps |
| [`roadmap`](#project-roadmap) | - | Read and update the issue dependency roadmap |
| [`run`](#project-run) | - | Run a configured project script in the foreground |
| [`self-update`](#project-self-update) | - | Check for and install a signed Project CLI release |
| [`serve`](#project-serve) | - | Run a project script with an explicit backend binding |
| [`status`](#project-status) | - | Show this machine's Project Space connection |
| [`template`](#project-template) | - | Manage the local project template snapshot |
| [`token`](#project-token) | - | Manage the Project 1Password token |
| [`validate`](#project-validate) | - | Validate a project against its template |
| [`workspace`](#project-workspace) | - | Manage one exact repository Workspace |
| [`worktree`](#project-worktree) | - | Prepare and validate isolated Codex worktrees |

## Command Reference

<h3 id="project-adopt" className="scroll-m-28"><code>project adopt</code></h3>

Plan adoption of an existing project

```sh
project adopt [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--dry-run` | `bool` | local | show the adoption plan without writing changes |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for adopt |
| `--module` | `string` | local | adopt a template module by adding only missing files |
| `--reason` | `string` | local | reason for a waiver |
| `--waive` | `string` | local | add a waiver for a path pattern |
| `-y`, `--yes` | `bool` | local | apply the adoption action without prompting |

<h3 id="project-agent" className="scroll-m-28"><code>project agent</code></h3>

Manage the current Codex agent identity

```sh
project agent
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for agent |

<h3 id="project-agent-name" className="scroll-m-28"><code>project agent name</code></h3>

Claim a unique Codex agent name, with an offline fallback

```sh
project agent name
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--exclude` | `stringArray` | local | agent name already used by a visible Codex task (repeatable) |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for name |

<h3 id="project-chat" className="scroll-m-28"><code>project chat</code></h3>

Coordinate with people and agents through Project Chat

```sh
project chat
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for chat |

<h3 id="project-chat-claim" className="scroll-m-28"><code>project chat claim</code></h3>

Claim an available registry name for this Codex thread

```sh
project chat claim <registry-name>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for claim |
| `--parent-thread` | `string` | local | main-agent thread ID (required for specialist names) |

<h3 id="project-chat-names" className="scroll-m-28"><code>project chat names</code></h3>

List Project Chat registry names and availability

```sh
project chat names
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for names |

<h3 id="project-chat-read" className="scroll-m-28"><code>project chat read</code></h3>

Read unread messages from #general

```sh
project chat read
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for read |

<h3 id="project-chat-send" className="scroll-m-28"><code>project chat send</code></h3>

Send one message to #general

```sh
project chat send <message>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for send |

<h3 id="project-codex" className="scroll-m-28"><code>project codex</code></h3>

Start, inspect, continue, or attach to persistent Codex tasks

```sh
project codex
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for codex |

<h3 id="project-codex-attach" className="scroll-m-28"><code>project codex attach</code></h3>

Attach the Codex TUI to an existing persistent thread

```sh
project codex attach
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--codex-binary` | `string` | local | explicit Codex CLI binary path |
| `--connector` | `string` | local | exact connector installation ID |
| `-h`, `--help` | `bool` | local | help for attach |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |
| `--operation-id` | `string` | local | stable idempotency key for safe retries |
| `--thread` | `string` | local | persistent Codex thread ID |

<h3 id="project-codex-login" className="scroll-m-28"><code>project codex login</code></h3>

Authorize managed Codex on a remote WSL connector

```sh
project codex login
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--cancel` | `bool` | local | cancel the selected authorization attempt |
| `--connector` | `string` | local | exact connector installation ID |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for login |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |
| `--no-wait` | `bool` | local | return after creating the device code |
| `--operation-id` | `string` | local | stable idempotency key for start, status, and cancellation |
| `--status` | `bool` | local | read the selected authorization attempt |

<h3 id="project-codex-read" className="scroll-m-28"><code>project codex read</code></h3>

Read a persistent Codex thread non-interactively

```sh
project codex read
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--connector` | `string` | local | exact connector installation ID |
| `--format` | `string` | local | output format: json |
| `-h`, `--help` | `bool` | local | help for read |
| `--last` | `int` | local | return only the last N turns |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |
| `--thread` | `string` | local | persistent Codex thread ID |

<h3 id="project-codex-send" className="scroll-m-28"><code>project codex send</code></h3>

Send a non-interactive turn to a persistent Codex thread

```sh
project codex send
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--connector` | `string` | local | exact connector installation ID |
| `--format` | `string` | local | output format: json or ndjson |
| `-h`, `--help` | `bool` | local | help for send |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |
| `--message` | `string` | local | turn prompt, or - to read from standard input |
| `--no-wait` | `bool` | local | return as soon as the turn is accepted |
| `--operation-id` | `string` | local | stable idempotency key for safe retries |
| `--prompt-file` | `string` | local | read the turn prompt from a regular file |
| `--thread` | `string` | local | persistent Codex thread ID |
| `--wait` | `bool` | local | wait for the turn to complete or require interaction |

<h3 id="project-codex-start" className="scroll-m-28"><code>project codex start</code></h3>

Start a persistent Codex task from a GitHub issue

```sh
project codex start
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--connector` | `string` | local | exact connector installation ID |
| `--dry-run` | `bool` | local | resolve the exact target without starting a task |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for start |
| `--here` | `bool` | local | use the physical machine connected by this CLI |
| `--issue` | `int` | local | GitHub issue number |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |
| `--operation-id` | `string` | local | stable idempotency key for safe retries |
| `--repository` | `string` | local | exact GitHub owner/name or repository ID |

<h3 id="project-completion" className="scroll-m-28"><code>project completion</code></h3>

Generate the autocompletion script for the specified shell

```sh
project completion
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for completion |

<h3 id="project-completion-bash" className="scroll-m-28"><code>project completion bash</code></h3>

Generate the autocompletion script for bash

```sh
project completion bash
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for bash |
| `--no-descriptions` | `bool` | local | disable completion descriptions |

<h3 id="project-completion-fish" className="scroll-m-28"><code>project completion fish</code></h3>

Generate the autocompletion script for fish

```sh
project completion fish
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for fish |
| `--no-descriptions` | `bool` | local | disable completion descriptions |

<h3 id="project-completion-powershell" className="scroll-m-28"><code>project completion powershell</code></h3>

Generate the autocompletion script for powershell

```sh
project completion powershell
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for powershell |
| `--no-descriptions` | `bool` | local | disable completion descriptions |

<h3 id="project-completion-zsh" className="scroll-m-28"><code>project completion zsh</code></h3>

Generate the autocompletion script for zsh

```sh
project completion zsh
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for zsh |
| `--no-descriptions` | `bool` | local | disable completion descriptions |

<h3 id="project-connect" className="scroll-m-28"><code>project connect</code></h3>

Connect this machine to Project Space

```sh
project connect
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--connector-mode` | `string` | local | connector lifecycle: managed or foreground |
| `-h`, `--help` | `bool` | local | help for connect |
| `--json` | `bool` | local | print machine-readable output |
| `--name` | `string` | local | machine name shown in Project Space |
| `--no-open` | `bool` | local | print the approval URL without opening a browser |

<h3 id="project-connector" className="scroll-m-28"><code>project connector</code></h3>

Retired compatibility command

```sh
project connector
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for connector |
| `--json` | `bool` | local | print the retirement response as JSON |

<h3 id="project-control" className="scroll-m-28"><code>project control</code></h3>

Run typed control-gateway operations

```sh
project control
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for control |

<h3 id="project-control-handshake" className="scroll-m-28"><code>project control handshake</code></h3>

Describe the installed control protocol

```sh
project control handshake
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for handshake |

<h3 id="project-control-status" className="scroll-m-28"><code>project control status</code></h3>

Verify one Environment through the typed SSH gateway

```sh
project control status <environment-instance>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for status |
| `--json` | `bool` | local | print machine-readable JSON |
| `--operation-id` | `string` | local | reuse one exact idempotent operation ID |

<h3 id="project-create" className="scroll-m-28"><code>project create</code></h3>

Create and initialize a new project

```sh
project create [directory]
```

Aliases: `new`

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--commit` | `string` | local | template commit or label |
| `--force` | `bool` | local | replace an existing .project/template.lock.yaml |
| `--github` | `bool` | local | create a private GitHub repository and push the project |
| `--github-visibility` | `string` | local | GitHub repository visibility |
| `--global-tmp` | `bool` | local | create a global tmp project in /tmp and install default modules |
| `-h`, `--help` | `bool` | local | help for create |
| `--local-tmp` | `bool` | local | create a local tmp project in ./tmp and install default modules |
| `--target` | `stringArray` | local | app target and devices (&lt;target&gt;:&lt;device&gt;[,&lt;device&gt;...]); repeat for multiple targets |
| `--template` | `string` | local | template repository |
| `--template-path` | `string` | local | template path |
| `--tmp` | `bool` | local | create a local tmp project in ./tmp and install default modules |
| `--version` | `string` | local | template version |

<h3 id="project-deploy" className="scroll-m-28"><code>project deploy</code></h3>

Deploy this project to the VPS

```sh
project deploy [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--api-domain` | `string` | local | project API domain |
| `--branch` | `string` | local | git branch to deploy |
| `--commit` | `string` | local | full Git commit SHA to deploy |
| `--domain` | `string` | local | project domain |
| `--dry-run` | `bool` | local | print planned remote actions without changing the VPS |
| `--email` | `string` | local | Traefik ACME email |
| `--env` | `string` | local | deployment environment: prod or beta |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for deploy |
| `--host` | `string` | local | SSH host |
| `--lock-timeout` | `duration` | local | maximum time to wait for the remote deployment lock |
| `--path` | `string` | local | remote project directory |
| `--release-version` | `string` | local | signed release version approved for this commit |

<h3 id="project-deploy-preview" className="scroll-m-28"><code>project deploy preview</code></h3>

Deploy a pull request through the trusted preview workflow

```sh
project deploy preview [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for preview |
| `--inventory-revision` | `string` | local | current Preview inventory revision for a confirmed replacement |
| `--pr` | `int` | local | GitHub pull request number |
| `--replace-head-sha` | `string` | local | exact head SHA of the selected replacement Preview |
| `--replace-pr` | `int` | local | online Preview PR selected for explicit replacement |
| `--replace-repository` | `string` | local | repository of the selected replacement Preview |

<h3 id="project-deploy-preview-destroy" className="scroll-m-28"><code>project deploy preview destroy</code></h3>

Remove a pull request preview through the trusted workflow

```sh
project deploy preview destroy [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for destroy |
| `--inventory-revision` | `string` | local | current Preview inventory revision for a confirmed replacement |
| `--pr` | `int` | local | GitHub pull request number |
| `--replace-head-sha` | `string` | local | exact head SHA of the selected replacement Preview |
| `--replace-pr` | `int` | local | online Preview PR selected for explicit replacement |
| `--replace-repository` | `string` | local | repository of the selected replacement Preview |

<h3 id="project-deploy-preview-start" className="scroll-m-28"><code>project deploy preview start</code></h3>

Explicitly start an on-demand pull request Preview

```sh
project deploy preview start [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for start |
| `--inventory-revision` | `string` | local | current Preview inventory revision for a confirmed replacement |
| `--pr` | `int` | local | GitHub pull request number |
| `--replace-head-sha` | `string` | local | exact head SHA of the selected replacement Preview |
| `--replace-pr` | `int` | local | online Preview PR selected for explicit replacement |
| `--replace-repository` | `string` | local | repository of the selected replacement Preview |

<h3 id="project-deploy-preview-status" className="scroll-m-28"><code>project deploy preview status</code></h3>

Read verified pull request preview status from the VPS

```sh
project deploy preview status [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--all` | `bool` | local | show all pull request previews for this repository |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for status |
| `--inventory-revision` | `string` | local | current Preview inventory revision for a confirmed replacement |
| `--pr` | `int` | local | GitHub pull request number |
| `--replace-head-sha` | `string` | local | exact head SHA of the selected replacement Preview |
| `--replace-pr` | `int` | local | online Preview PR selected for explicit replacement |
| `--replace-repository` | `string` | local | repository of the selected replacement Preview |

<h3 id="project-deploy-preview-stop" className="scroll-m-28"><code>project deploy preview stop</code></h3>

Explicitly stop an on-demand pull request Preview

```sh
project deploy preview stop [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for stop |
| `--inventory-revision` | `string` | local | current Preview inventory revision for a confirmed replacement |
| `--pr` | `int` | local | GitHub pull request number |
| `--replace-head-sha` | `string` | local | exact head SHA of the selected replacement Preview |
| `--replace-pr` | `int` | local | online Preview PR selected for explicit replacement |
| `--replace-repository` | `string` | local | repository of the selected replacement Preview |

<h3 id="project-deploy-preview-touch" className="scroll-m-28"><code>project deploy preview touch</code></h3>

Explicitly touch an on-demand pull request Preview

```sh
project deploy preview touch [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for touch |
| `--inventory-revision` | `string` | local | current Preview inventory revision for a confirmed replacement |
| `--pr` | `int` | local | GitHub pull request number |
| `--replace-head-sha` | `string` | local | exact head SHA of the selected replacement Preview |
| `--replace-pr` | `int` | local | online Preview PR selected for explicit replacement |
| `--replace-repository` | `string` | local | repository of the selected replacement Preview |

<h3 id="project-deploy-status" className="scroll-m-28"><code>project deploy status</code></h3>

Inspect deployment status without changing the VPS

```sh
project deploy status [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--all-envs` | `bool` | local | inspect all configured environments |
| `--api-domain` | `string` | local | project API domain |
| `--branch` | `string` | local | git branch to deploy |
| `--commit` | `string` | local | full Git commit SHA to deploy |
| `--domain` | `string` | local | project domain |
| `--email` | `string` | local | Traefik ACME email |
| `--env` | `string` | local | deployment environment: prod or beta |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for status |
| `--host` | `string` | local | SSH host |
| `--lock-timeout` | `duration` | local | maximum time to wait for the remote deployment lock |
| `--path` | `string` | local | remote project directory |
| `--release-version` | `string` | local | signed release version approved for this commit |

<h3 id="project-dev-build" className="scroll-m-28"><code>project dev-build</code></h3>

Build short-lived development tools for an exact pull request head

```sh
project dev-build
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for dev-build |

<h3 id="project-dev-build-create" className="scroll-m-28"><code>project dev-build create</code></h3>

Request unsigned PR development tools

```sh
project dev-build create [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for create |
| `--platform` | `stringSlice` | local | development platform: linux-x64, macos-arm64, or windows-x64 |
| `--pr` | `int` | local | GitHub pull request number |

<h3 id="project-disconnect" className="scroll-m-28"><code>project disconnect</code></h3>

Disconnect this machine from Project Space

```sh
project disconnect
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--connector-mode` | `string` | local | connector lifecycle: managed or foreground |
| `-h`, `--help` | `bool` | local | help for disconnect |
| `--json` | `bool` | local | print machine-readable output |

<h3 id="project-doctor" className="scroll-m-28"><code>project doctor</code></h3>

Diagnose local or managed-machine Project Space readiness

```sh
project doctor
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--connector` | `string` | local | exact connector installation ID |
| `--fix` | `bool` | local | apply the exact confirmed managed repair plan |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for doctor |
| `--json` | `bool` | local | print machine-readable output |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |
| `--yes` | `bool` | local | confirm the current exact repair plan non-interactively |

<h3 id="project-environment" className="scroll-m-28"><code>project environment</code></h3>

Discover Environment definitions and instances

```sh
project environment
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for environment |

<h3 id="project-environment-bootstrap" className="scroll-m-28"><code>project environment bootstrap</code></h3>

Start a pinned Workspace Runtime without installing a permanent Connector

```sh
project environment bootstrap <environment-instance>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--branch` | `string` | local | expected Workspace branch |
| `--commit` | `string` | local | expected immutable Workspace commit |
| `--format` | `string` | local | output format: text or json |
| `--generation` | `string` | local | new exact Runtime generation UUID |
| `-h`, `--help` | `bool` | local | help for bootstrap |
| `--manifest-digest` | `string` | local | pinned Runtime manifest SHA-256 |
| `--mode` | `string` | local | Runtime isolation: process or devcontainer |
| `--operation-id` | `string` | local | stable idempotency identity |
| `--profile` | `string` | local | Runtime profile: codex, inspection, or mutation |
| `--runtime-version` | `string` | local | pinned Project Runtime version |
| `--workspace` | `string` | local | exact canonical Workspace ID |
| `--worktree-owner-thread` | `string` | local | exact managed Worktree owner task UUID (mutation profile only) |

<h3 id="project-environment-instance" className="scroll-m-28"><code>project environment instance</code></h3>

Discover concrete Environment Instances

```sh
project environment instance
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for instance |

<h3 id="project-environment-instance-list" className="scroll-m-28"><code>project environment instance list</code></h3>

List concrete Environment Instances

```sh
project environment instance list
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--environment` | `string` | local | exact Environment definition selector |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for list |
| `--host` | `string` | local | exact host selector |
| `--platform` | `string` | local | exact platform selector |

<h3 id="project-environment-instance-show" className="scroll-m-28"><code>project environment instance show</code></h3>

Show one concrete Environment Instance

```sh
project environment instance show <instance-ref>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for show |

<h3 id="project-environment-list" className="scroll-m-28"><code>project environment list</code></h3>

List reusable Environment definitions

```sh
project environment list
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for list |

<h3 id="project-environment-show" className="scroll-m-28"><code>project environment show</code></h3>

Show one Environment definition

```sh
project environment show <environment-definition>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for show |

<h3 id="project-help" className="scroll-m-28"><code>project help</code></h3>

Help about any command

```sh
project help [command]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for help |

<h3 id="project-host" className="scroll-m-28"><code>project host</code></h3>

Discover physical or virtual hosts

```sh
project host
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for host |

<h3 id="project-host-list" className="scroll-m-28"><code>project host list</code></h3>

List inventory hosts

```sh
project host list
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for list |
| `--platform` | `string` | local | exact platform ID, name, kind, or alias |

<h3 id="project-host-show" className="scroll-m-28"><code>project host show</code></h3>

Show one exact inventory host

```sh
project host show <host>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for show |

<h3 id="project-init" className="scroll-m-28"><code>project init</code></h3>

Initialize a project template lock

```sh
project init [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--commit` | `string` | local | template commit or label |
| `--force` | `bool` | local | replace an existing .project/template.lock.yaml |
| `-h`, `--help` | `bool` | local | help for init |
| `--template` | `string` | local | template repository |
| `--template-path` | `string` | local | template path |
| `--version` | `string` | local | template version |

<h3 id="project-inventory" className="scroll-m-28"><code>project inventory</code></h3>

Discover the complete compute inventory

```sh
project inventory
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for inventory |
| `--json` | `bool` | local | print machine-readable JSON |

<h3 id="project-list" className="scroll-m-28"><code>project list</code></h3>

List account projects and local availability

```sh
project list
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for list |

<h3 id="project-machine" className="scroll-m-28"><code>project machine</code></h3>

Manage exact physical machines

```sh
project machine
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for machine |

<h3 id="project-machine-list" className="scroll-m-28"><code>project machine list</code></h3>

Deprecated compatibility: list inventory hosts

```sh
project machine list
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for list |
| `--platform` | `string` | local | exact platform ID, name, kind, or alias |

<h3 id="project-machine-power" className="scroll-m-28"><code>project machine power</code></h3>

Inspect or request managed physical power

```sh
project machine power
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for power |

<h3 id="project-machine-power-off" className="scroll-m-28"><code>project machine power off</code></h3>

Request the exact managed machine to power off

```sh
project machine power off
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for off |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |

<h3 id="project-machine-power-on" className="scroll-m-28"><code>project machine power on</code></h3>

Request the exact managed machine to power on

```sh
project machine power on
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for on |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |
| `--no-wait` | `bool` | local | return after the provider delivery attempt |
| `--timeout` | `duration` | local | maximum time to wait for physical confirmation |

<h3 id="project-machine-power-status" className="scroll-m-28"><code>project machine power status</code></h3>

Show fresh managed physical-power evidence

```sh
project machine power status
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for status |
| `--machine` | `string` | local | exact physical machine name |
| `--machine-id` | `string` | local | exact physical machine ID |

<h3 id="project-machine-show" className="scroll-m-28"><code>project machine show</code></h3>

Deprecated compatibility: show one inventory host

```sh
project machine show <host>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for show |

<h3 id="project-module" className="scroll-m-28"><code>project module</code></h3>

Manage project template modules

```sh
project module
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for module |

<h3 id="project-module-add" className="scroll-m-28"><code>project module add</code></h3>

Add a project template module

```sh
project module add <module> [directory]
```

Aliases: `install`

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--dry-run` | `bool` | local | show the module add plan without writing changes |
| `--force` | `bool` | local | allow module add to overwrite existing project files |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for add |
| `-y`, `--yes` | `bool` | local | apply the module add plan without prompting |

<h3 id="project-module-list" className="scroll-m-28"><code>project module list</code></h3>

List project template modules

```sh
project module list [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for list |

<h3 id="project-module-remove" className="scroll-m-28"><code>project module remove</code></h3>

Remove a project template module

```sh
project module remove <module> [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--dry-run` | `bool` | local | show the module remove plan without writing changes |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for remove |
| `-y`, `--yes` | `bool` | local | apply the module remove plan without prompting |

<h3 id="project-module-show" className="scroll-m-28"><code>project module show</code></h3>

Show details for a project template module

```sh
project module show <module> [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for show |

<h3 id="project-open" className="scroll-m-28"><code>project open</code></h3>

Open a local project in the system's selected terminal

```sh
project open <project>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for open |

<h3 id="project-path" className="scroll-m-28"><code>project path</code></h3>

Print a project's canonical local path

```sh
project path <project>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: path or json |
| `-h`, `--help` | `bool` | local | help for path |

<h3 id="project-platform" className="scroll-m-28"><code>project platform</code></h3>

Discover compute capacity providers

```sh
project platform
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for platform |

<h3 id="project-platform-list" className="scroll-m-28"><code>project platform list</code></h3>

List configured compute platforms

```sh
project platform list
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for list |

<h3 id="project-platform-show" className="scroll-m-28"><code>project platform show</code></h3>

Show one exact compute platform

```sh
project platform show <platform>
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for show |

<h3 id="project-prepare" className="scroll-m-28"><code>project prepare</code></h3>

Run trusted repository setup steps

```sh
project prepare [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expect-commit` | `string` | local | require this exact repository commit before setup |
| `--expect-declaration-digest` | `string` | local | require this exact setup declaration digest before setup |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for prepare |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--step` | `string` | local | stable setup step ID (default: all steps in declaration order) |

<h3 id="project-prepare-status" className="scroll-m-28"><code>project prepare status</code></h3>

Inspect trusted repository setup

```sh
project prepare status [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expect-commit` | `string` | local | require this exact repository commit before setup |
| `--expect-declaration-digest` | `string` | local | require this exact setup declaration digest before setup |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for status |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--step` | `string` | local | stable setup step ID (default: all steps in declaration order) |

<h3 id="project-roadmap" className="scroll-m-28"><code>project roadmap</code></h3>

Read and update the issue dependency roadmap

```sh
project roadmap
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for roadmap |
| `--repository` | `string` | local | GitHub repository as owner/name (defaults to the current origin) |
| `--verbose` | `bool` | local | include issue titles and normalized descriptions |

<h3 id="project-roadmap-dependency" className="scroll-m-28"><code>project roadmap dependency</code></h3>

Update roadmap prerequisite relationships

```sh
project roadmap dependency
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | inherited | output format: text or json |
| `-h`, `--help` | `bool` | local | help for dependency |
| `--repository` | `string` | inherited | GitHub repository as owner/name (defaults to the current origin) |

<h3 id="project-roadmap-dependency-add" className="scroll-m-28"><code>project roadmap dependency add</code></h3>

Add an issue prerequisite

```sh
project roadmap dependency add [issue]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | inherited | output format: text or json |
| `-h`, `--help` | `bool` | local | help for add |
| `--repository` | `string` | inherited | GitHub repository as owner/name (defaults to the current origin) |
| `--requires` | `int` | local | prerequisite issue number |
| `--requires-repository` | `string` | local | prerequisite repository as owner/name (defaults to --repository) |

<h3 id="project-roadmap-dependency-remove" className="scroll-m-28"><code>project roadmap dependency remove</code></h3>

Remove an issue prerequisite

```sh
project roadmap dependency remove [issue]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | inherited | output format: text or json |
| `-h`, `--help` | `bool` | local | help for remove |
| `--repository` | `string` | inherited | GitHub repository as owner/name (defaults to the current origin) |
| `--requires` | `int` | local | prerequisite issue number |
| `--requires-repository` | `string` | local | prerequisite repository as owner/name (defaults to --repository) |

<h3 id="project-run" className="scroll-m-28"><code>project run</code></h3>

Run a configured project script in the foreground

```sh
project run <script> [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for run |
| `--json` | `bool` | local | print machine-readable JSON output |

<h3 id="project-self-update" className="scroll-m-28"><code>project self-update</code></h3>

Check for and install a signed Project CLI release

```sh
project self-update
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--check` | `bool` | local | check the signed approved release without changing files |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for self-update |
| `-y`, `--yes` | `bool` | local | install the verified update without prompting |

<h3 id="project-serve" className="scroll-m-28"><code>project serve</code></h3>

Run a project script with an explicit backend binding

```sh
project serve [script] [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--allowed-host` | `stringArray` | local | explicit Vite host allowed to reach this session (repeatable) |
| `--apis` | `string` | local | backend API binding: simulated or external |
| `--data` | `string` | local | backend data binding: local or remote |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for serve |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--local-only` | `bool` | local | deprecated alias for --no-tailnet |
| `--no-tailnet` | `bool` | local | keep the server on this machine instead of publishing it through Tailscale |
| `--tailnet` | `bool` | local | publish through Tailscale (the default; retained for compatibility) |

<h3 id="project-serve-attach" className="scroll-m-28"><code>project serve attach</code></h3>

Attach to the exact owned tmux session for one managed server

```sh
project serve attach [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for attach |
| `--script` | `string` | local | configured server name |

<h3 id="project-serve-list" className="scroll-m-28"><code>project serve list</code></h3>

List managed project server sessions

```sh
project serve list [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--configured` | `bool` | local | list declarations from one repository instead of runtime sessions |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for list |
| `--json` | `bool` | local | print machine-readable JSON output |

<h3 id="project-serve-logs" className="scroll-m-28"><code>project serve logs</code></h3>

Read the bounded log for one managed project server

```sh
project serve logs [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--follow` | `bool` | local | continue streaming new log output |
| `-h`, `--help` | `bool` | local | help for logs |
| `--script` | `string` | local | configured server name |

<h3 id="project-serve-publish-pr" className="scroll-m-28"><code>project serve publish-pr</code></h3>

Publish a managed dev server to one pull request

```sh
project serve publish-pr [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--branch` | `string` | local | expected checked-out branch |
| `--codex-thread` | `string` | local | exact Codex task eligible to receive feedback |
| `--commit` | `string` | local | expected checked-out commit |
| `-h`, `--help` | `bool` | local | help for publish-pr |
| `--machine-id` | `string` | local | Project Space physical machine ID |
| `--pr` | `int` | local | pull request number |
| `--project-id` | `string` | local | connector inventory project ID |
| `--repository` | `string` | local | GitHub owner/name |
| `--script` | `string` | local | configured prototype server |
| `--worktree-id` | `string` | local | connector inventory worktree ID |

<h3 id="project-serve-reconcile" className="scroll-m-28"><code>project serve reconcile</code></h3>

Check managed project servers and clean stale sessions

```sh
project serve reconcile
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for reconcile |
| `--json` | `bool` | local | print machine-readable JSON output |

<h3 id="project-serve-status" className="scroll-m-28"><code>project serve status</code></h3>

Inspect a managed project server

```sh
project serve status [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for status |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--script` | `string` | local | configured script name |

<h3 id="project-serve-stop" className="scroll-m-28"><code>project serve stop</code></h3>

Stop one managed project server

```sh
project serve stop [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for stop |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--script` | `string` | local | configured script name |

<h3 id="project-status" className="scroll-m-28"><code>project status</code></h3>

Show this machine's Project Space connection

```sh
project status
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for status |
| `--json` | `bool` | local | print machine-readable output |

<h3 id="project-template" className="scroll-m-28"><code>project template</code></h3>

Manage the local project template snapshot

```sh
project template
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for template |

<h3 id="project-template-lint" className="scroll-m-28"><code>project template lint</code></h3>

Validate a template checkout

```sh
project template lint
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for lint |
| `--template-path` | `string` | local | template checkout path |

<h3 id="project-template-smoke" className="scroll-m-28"><code>project template smoke</code></h3>

Generate a tmp project and run template smoke checks

```sh
project template smoke
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--commit` | `string` | local | template commit or label |
| `--container` | `bool` | local | build generated project containers |
| `--global-tmp` | `bool` | local | create the generated project under /tmp |
| `-h`, `--help` | `bool` | local | help for smoke |
| `--name` | `string` | local | tmp project name prefix |
| `--skip-checks` | `bool` | local | skip dependency, secrets, check, and build commands |
| `--skip-secrets-doctor` | `bool` | local | skip bun run secrets:doctor |
| `--target` | `stringArray` | local | app target and devices (&lt;target&gt;:&lt;device&gt;[,&lt;device&gt;...]); repeat for multiple targets |
| `--template` | `string` | local | template repository |
| `--template-path` | `string` | local | template path |
| `--version` | `string` | local | template version |

<h3 id="project-template-sync" className="scroll-m-28"><code>project template sync</code></h3>

Sync the local template snapshot

```sh
project template sync [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--dry-run` | `bool` | local | show the template sync plan without writing changes |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for sync |
| `--template-path` | `string` | local | template source path |

<h3 id="project-template-update" className="scroll-m-28"><code>project template update</code></h3>

Update a project from its template

```sh
project template update [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--dry-run` | `bool` | local | show the template update plan without writing changes |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for update |
| `--template-path` | `string` | local | template source path |
| `-y`, `--yes` | `bool` | local | apply the template update without prompting |

<h3 id="project-token" className="scroll-m-28"><code>project token</code></h3>

Manage the Project 1Password token

```sh
project token
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for token |

<h3 id="project-token-create" className="scroll-m-28"><code>project token create</code></h3>

Create the Project 1Password service account token

```sh
project token create
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--dry-run` | `bool` | local | show what would happen without creating a token |
| `--expires-in` | `string` | local | create a temporary token with a 1Password expiry, for example 24h |
| `-h`, `--help` | `bool` | local | help for create |
| `--json` | `bool` | local | print machine-readable output |
| `-y`, `--yes` | `bool` | local | create the token without prompting |

<h3 id="project-validate" className="scroll-m-28"><code>project validate</code></h3>

Validate a project against its template

```sh
project validate [directory|file]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--color-scope` | `string` | local | color scope |
| `--dry-run` | `bool` | local | show the quarantine plan without writing changes |
| `--format` | `string` | local | output format |
| `-h`, `--help` | `bool` | local | help for validate |
| `--quarantine` | `bool` | local | move not_allowed validation violations into .project/quarantine |
| `--status-color-only` | `bool` | local | color only the status text |
| `--view` | `string` | local | pretty output view |
| `-y`, `--yes` | `bool` | local | apply the quarantine plan without prompting |

<h3 id="project-workspace" className="scroll-m-28"><code>project workspace</code></h3>

Manage one exact repository Workspace

```sh
project workspace
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for workspace |

<h3 id="project-workspace-runtime" className="scroll-m-28"><code>project workspace runtime</code></h3>

Manage the Workspace's ephemeral runtime

```sh
project workspace runtime
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for runtime |

<h3 id="project-workspace-runtime-clean" className="scroll-m-28"><code>project workspace runtime clean</code></h3>

Remove only stopped generation-owned runtime state

```sh
project workspace runtime clean [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expected-commit` | `string` | local | exact approved Workspace HEAD |
| `--expected-digest` | `string` | local | exact resolved runtime manifest digest |
| `--expected-generation` | `string` | local | exact runtime generation for lifecycle fencing |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for clean |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--mode` | `string` | local | runtime provider: process or devcontainer |
| `--thread-id` | `string` | local | exact Project-managed Worktree owner (defaults to CODEX_THREAD_ID) |

<h3 id="project-workspace-runtime-inspect" className="scroll-m-28"><code>project workspace runtime inspect</code></h3>

Inspect exact runtime ownership

```sh
project workspace runtime inspect [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expected-commit` | `string` | local | exact approved Workspace HEAD |
| `--expected-digest` | `string` | local | exact resolved runtime manifest digest |
| `--expected-generation` | `string` | local | exact runtime generation for lifecycle fencing |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for inspect |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--mode` | `string` | local | runtime provider: process or devcontainer |
| `--thread-id` | `string` | local | exact Project-managed Worktree owner (defaults to CODEX_THREAD_ID) |

<h3 id="project-workspace-runtime-reconcile" className="scroll-m-28"><code>project workspace runtime reconcile</code></h3>

Repair an interrupted runtime only from exact ownership evidence

```sh
project workspace runtime reconcile [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expected-commit` | `string` | local | exact approved Workspace HEAD |
| `--expected-digest` | `string` | local | exact resolved runtime manifest digest |
| `--expected-generation` | `string` | local | exact runtime generation for lifecycle fencing |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for reconcile |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--mode` | `string` | local | runtime provider: process or devcontainer |
| `--thread-id` | `string` | local | exact Project-managed Worktree owner (defaults to CODEX_THREAD_ID) |

<h3 id="project-workspace-runtime-resume" className="scroll-m-28"><code>project workspace runtime resume</code></h3>

Resume the exact Workspace runtime

```sh
project workspace runtime resume [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expected-commit` | `string` | local | exact approved Workspace HEAD |
| `--expected-digest` | `string` | local | exact resolved runtime manifest digest |
| `--expected-generation` | `string` | local | exact runtime generation for lifecycle fencing |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for resume |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--mode` | `string` | local | runtime provider: process or devcontainer |
| `--thread-id` | `string` | local | exact Project-managed Worktree owner (defaults to CODEX_THREAD_ID) |

<h3 id="project-workspace-runtime-retention" className="scroll-m-28"><code>project workspace runtime retention</code></h3>

Inspect or reclaim proof-bound retained Runtime archives

```sh
project workspace runtime retention
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for retention |

<h3 id="project-workspace-runtime-retention-collect" className="scroll-m-28"><code>project workspace runtime retention collect</code></h3>

Reclaim eligible archives inside an exclusive collector boundary

```sh
project workspace runtime retention collect
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--collector-root` | `string` | local | existing private collector-owned root on the same filesystem |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for collect |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--maximum-bytes` | `int64` | local | maximum archive bytes reclaimed in one run |
| `--minimum-age` | `duration` | local | minimum age of terminal evidence and retained archive |
| `--source-root` | `string` | local | exact Workspace Runtime state root owned by the Workspace user |

<h3 id="project-workspace-runtime-retention-status" className="scroll-m-28"><code>project workspace runtime retention status</code></h3>

Inspect retained Runtime archives without deleting them

```sh
project workspace runtime retention status
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--collector-root` | `string` | local | existing private collector-owned root on the same filesystem |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for status |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--maximum-bytes` | `int64` | local | maximum archive bytes reclaimed in one run |
| `--minimum-age` | `duration` | local | minimum age of terminal evidence and retained archive |
| `--source-root` | `string` | local | exact Workspace Runtime state root owned by the Workspace user |

<h3 id="project-workspace-runtime-start" className="scroll-m-28"><code>project workspace runtime start</code></h3>

Start or reuse an exact Workspace runtime

```sh
project workspace runtime start [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expected-commit` | `string` | local | exact approved Workspace HEAD |
| `--expected-digest` | `string` | local | exact resolved runtime manifest digest |
| `--expected-generation` | `string` | local | exact runtime generation for lifecycle fencing |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for start |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--mode` | `string` | local | runtime provider: process or devcontainer |
| `--thread-id` | `string` | local | exact Project-managed Worktree owner (defaults to CODEX_THREAD_ID) |

<h3 id="project-workspace-runtime-stop" className="scroll-m-28"><code>project workspace runtime stop</code></h3>

Stop the exact Workspace runtime without deleting its checkout

```sh
project workspace runtime stop [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expected-commit` | `string` | local | exact approved Workspace HEAD |
| `--expected-digest` | `string` | local | exact resolved runtime manifest digest |
| `--expected-generation` | `string` | local | exact runtime generation for lifecycle fencing |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for stop |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--mode` | `string` | local | runtime provider: process or devcontainer |
| `--thread-id` | `string` | local | exact Project-managed Worktree owner (defaults to CODEX_THREAD_ID) |

<h3 id="project-workspace-runtime-suspend" className="scroll-m-28"><code>project workspace runtime suspend</code></h3>

Suspend the exact Workspace runtime

```sh
project workspace runtime suspend [directory]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expected-commit` | `string` | local | exact approved Workspace HEAD |
| `--expected-digest` | `string` | local | exact resolved runtime manifest digest |
| `--expected-generation` | `string` | local | exact runtime generation for lifecycle fencing |
| `--format` | `string` | local | output format: pretty or json |
| `-h`, `--help` | `bool` | local | help for suspend |
| `--json` | `bool` | local | print machine-readable JSON output |
| `--mode` | `string` | local | runtime provider: process or devcontainer |
| `--thread-id` | `string` | local | exact Project-managed Worktree owner (defaults to CODEX_THREAD_ID) |

<h3 id="project-worktree" className="scroll-m-28"><code>project worktree</code></h3>

Prepare and validate isolated Codex worktrees

```sh
project worktree
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `-h`, `--help` | `bool` | local | help for worktree |

<h3 id="project-worktree-check" className="scroll-m-28"><code>project worktree check</code></h3>

Validate that the current Codex thread owns this isolated worktree

```sh
project worktree check
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for check |

<h3 id="project-worktree-materialize" className="scroll-m-28"><code>project worktree materialize</code></h3>

Materialize a server-approved GitHub branch in the Project-managed worktree root

```sh
project worktree materialize
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--branch` | `string` | local | server-approved exact branch identity |
| `--commit` | `string` | local | server-approved exact commit |
| `--format` | `string` | local | output format: json |
| `-h`, `--help` | `bool` | local | help for materialize |
| `--repository` | `string` | local | server-approved GitHub owner/name |

<h3 id="project-worktree-prepare" className="scroll-m-28"><code>project worktree prepare</code></h3>

Prepare the worktree owned by the current Codex thread

```sh
project worktree prepare [task-name]
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for prepare |
| `--issue` | `int` | local | prepare a worktree for an existing open GitHub issue |

<h3 id="project-worktree-recover" className="scroll-m-28"><code>project worktree recover</code></h3>

Replace a confirmed orphaned Codex owner on a pristine managed worktree

```sh
project worktree recover
```

| Flag | Type | Scope | Description |
| --- | --- | --- | --- |
| `--expected-owner` | `string` | local | exact orphaned Codex thread id |
| `--format` | `string` | local | output format: text or json |
| `-h`, `--help` | `bool` | local | help for recover |

## Guides

- [Project Doctor](/docs/cli/doctor)
- [Self-update](/docs/cli/self-update)
- [Create and init](/docs/cli/create-init)
- [Validate](/docs/cli/validate)
- [Modules](/docs/cli/modules)
- [Template commands](/docs/cli/template)
- [Deploy](/docs/cli/deploy)
- [Shell completion](/docs/cli/completion)

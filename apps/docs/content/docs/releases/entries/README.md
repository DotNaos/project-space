# Historical release entries

These files are immutable history from the former hand-authored release flow.
New pull requests must add `changelog/<PR-number>.md` at the repository root;
they do not add or edit files in this directory. The raw changelog remains
visible in the Docs **Unreleased** Inbox until it is assigned to a published
version.

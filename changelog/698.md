---
bump: patch
---

# Remove legacy Connector command surfaces

Remove the retired machine Explorer and Project CLI command panels, stop remote filesystem, terminal, worktree, prototype, and historical workspace-command dispatch through the Connector, and preserve direct local and canonical Runtime paths.

---
bump: patch
---

# Make retention collector tests deterministic

Retention collector fixtures now use fixed archive timestamps alongside their
fixed collector clock, so age checks remain stable without changing production
retention safety behavior.

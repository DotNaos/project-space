## Canonical Environment bootstrap

- Start pinned Workspace Runtimes through the typed Project CLI without installing a permanent Connector.
- Make Environment bootstrap primary and keep Connector installation behind an explicit, expiring compatibility boundary.
- Ensure fresh machine-tools installs do not silently create or start a Connector service.

---
bump: minor
---

# Group Compute resources by their source of truth

The Compute page now shows deployment-owned Tailscale devices and GitHub Codespaces in
separate, independently refreshed sections. Tailscale rows keep their direct network addresses
and inline classification controls, while Codespaces show their provider-owned lifecycle,
repository, and branch without mixing in legacy Connector hosts or duplicate environments.

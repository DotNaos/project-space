---
bump: patch
---

# Restore remote Codex chat controls

Managed Linux connectors now find their bundled Codex runtime reliably, so the
model selector works in Codespaces. The chat composer also sends with Enter,
keeps Shift+Enter for new lines, and remains safe while composing text.

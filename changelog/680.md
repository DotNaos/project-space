---
bump: patch
---

# Remove the legacy Connector CLI implementation

Delete the retired Connector installer, source runner, supervisor and hidden
service implementation from the Project CLI. Old `project connector` calls keep
the stable `canonical_runtime_required` response, while canonical Workspace
Runtimes continue to use the dedicated Codex host.

## Optional host telemetry

- Add an outbound-only `project-hostd` that reports bounded host health and resource evidence without adding remote-control authority.
- Keep private-network and SSH workflows fully usable when the optional daemon is absent.
- Expose fresh, partial, stale, and unavailable telemetry states through the versioned Project CLI compute inventory.

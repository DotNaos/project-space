## Connector retirement ledger

- Record every legacy Connector responsibility with one canonical replacement owner and focused delivery issue.
- Keep compatibility aliases behind the same resolver and authorization boundary as canonical requests.
- Keep permanent removal blocked until deployed runtime, rollback, and a complete zero-use observation window are proven.

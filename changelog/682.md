---
bump: patch
---

# Remove hidden Connector API handlers

Retired Connector registry, credential, installer, Preview dev-server, and machine-runtime requests now terminate only at the stable migration boundary. Their former hidden handlers are no longer constructed behind that boundary.

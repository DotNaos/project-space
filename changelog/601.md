---
bump: patch
---

# Remove the legacy Electron shell

Project Space no longer installs or builds the unused Electron desktop shell
and its standalone React DevTools integration. The supported web, connector,
and mobile paths continue unchanged with a smaller root dependency tree.

---
bump: patch
---

# Fix preview deploys failing with "gh: executable file not found"

The trusted-runtime server has no `gh` CLI installed, so every "Deploy
preview" click on the Pull Request Previews page failed while loading GitHub
repository permissions. The preview CLI now calls the GitHub REST API
directly over HTTPS instead of shelling out to `gh`, authenticated with the
caller's already-connected GitHub OAuth token.

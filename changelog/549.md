---
bump: patch
---

# Preserve managed Codespace runner names

Managed connectors now publish the exact machine name that was approved by
the user instead of replacing it with an internal container name. GitHub
Codespaces therefore bind to their issue runner reliably and appear under
their concrete provider name.

---
bump: patch
---

# Restore the canonical Compute inventory

Compute now reads the owner-scoped canonical inventory directly, so existing
GitHub Codespaces remain visible even when the retired Connector overview is
unavailable. The page presents Platforms, optional Hosts, Environment
Instances, and Workspace Runtimes with separate status and honest loading,
empty, unavailable, and stale states.

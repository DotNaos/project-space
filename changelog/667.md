---
bump: patch
---

# Restore native Windows CLI compatibility

The native Windows CLI keeps its owner-side control features while directing Unix-only Workspace Runtime operations to the installed WSL environment.

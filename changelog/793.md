---
bump: patch
---

# Share fast local and CI quality checks

Project Space now checks staged changes before commit and uses the same named
quality checks locally and in CI, so developers get faster feedback without
changing the protected remote delivery gates.

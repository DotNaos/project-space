---
bump: patch
---

# Share fast local and CI quality checks

Project Space now checks staged changes before commit and uses the same named
quality checks locally and in CI. Developers can choose a fast changed-path
preflight or an optional comprehensive pass over local CI and release checks,
without changing the protected remote delivery gates.

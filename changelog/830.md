---
bump: minor
---

# Add client-owned Tailscale SSH access

Project Space now provides safe, project-scoped SSH launch metadata for fresh
Tailscale routes and a local `project ssh --environment-id` command. The local
client owns Tailscale reachability, host-key verification, credentials, the SSH
process, and terminal traffic; Project Space does not relay SSH sessions.

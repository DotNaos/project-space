---
bump: patch
---

# Delete the retired Connector installer

Remove the legacy Connector installer generator and its compatibility-window policy. Keep only the neutral public-origin validation used by current OAuth and control-gateway flows.

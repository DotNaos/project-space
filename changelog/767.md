---
bump: patch
---

# Reclaim clean worktrees backed by open pull requests

The Project CLI can now remove an inactive clean worktree before its pull
request merges when the exact head is preserved by the same GitHub repository.
The behavior requires an explicit opt-in, while merged pull requests remain the
default safety boundary.

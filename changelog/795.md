---
bump: patch
---

# Hide empty Compute platforms

Compute now hides platform groups that have no visible Environments, so retired
local Connector records do not leave empty Host-only sections behind.

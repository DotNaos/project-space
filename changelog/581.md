---
bump: patch
---

# Keep managed Codespaces tasks on the local path

Managed Codespaces tasks now use the local Project CLI for worktree and
parallel-task checks. They no longer call back into Project Space through the
same active connector and stall their own work.

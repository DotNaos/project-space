---
bump: patch
---

# Stabilize the managed-output safety regression test

The managed-output symlink safety test now fixes the foreign target's mode
after applying the process umask. This keeps the regression deterministic while
leaving the fail-closed production file-opening path unchanged.

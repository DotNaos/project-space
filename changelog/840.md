---
bump: patch
---

# Reconcile duplicate built-in Environment definitions

Equivalent built-in Environment definitions now reconcile deterministically before inventory validation. User-owned or incompatible definitions remain explicit conflicts, while exact physical-machine Codex targeting continues to use the selected host identity.

---
bump: patch
---

# Keep Codespace creation in a valid region

Project Space now falls back to West Europe when GitHub cannot return a
recommended Codespaces location. New task runners no longer fall back to an
uncontrolled region when that optional lookup is unavailable.

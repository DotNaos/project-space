---
bump: patch
---

# Make new Codespaces reachable during provisioning

New GitHub Codespaces now install their SSH service before GitHub completes
provisioning. This lets Project Space connect the managed runner immediately
instead of leaving a newly created Codespace permanently disconnected.

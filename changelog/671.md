---
bump: patch
---

# Bind release evidence to exact workflow attempts

Release recovery now verifies the immutable workflow attempts recorded by a
tombstone, so a later rerun of the same GitHub Actions run cannot invalidate
previously accepted evidence.

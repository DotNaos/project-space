---
bump: patch
---

# Recover the blocked Codex host release

Record the immutable failed v0.21.5 release after independently confirming its
source-embedded Linux and Windows packaging checks cannot publish. The release
queue can then publish the repaired Codex host packages from the next version.

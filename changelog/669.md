---
bump: patch
---

# Preserve failed immutable release reservations

The trusted release queue can now retain an unpublished failed tag as permanent
evidence while moving every queued change into one newly versioned, fully
verified catch-up release. Failed tags are never moved, deleted, or published
from different source.

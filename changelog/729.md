---
bump: patch
---

# Restore production Tailscale inventory access

The production Tailscale status service now uses the LocalAPI host identity
required by tailscaled, allowing its health check and inventory reads to
succeed without changing the fixed socket, route, or response boundary.

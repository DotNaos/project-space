---
bump: patch
---

# Preserve Environment ownership during reconciliation

Compute inventory reconciliation now keeps owner provenance while collapsing equivalent
built-in Environment definitions. A deployment definition that shares a UUID with a
user-defined definition no longer remaps or removes the user's Environment evidence.

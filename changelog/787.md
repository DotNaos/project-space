---
bump: minor
---

# Make Tailscale deployment-owned

Project Space deployments can now read their shared Tailnet inventory through
one deployment-managed OAuth connection. Everyone admitted to the same
single-tenant deployment sees the same classified devices, while credentials
remain outside the browser and application database.

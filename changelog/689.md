---
bump: patch
---

# Stop installing a permanent Connector service

Keep `project connect` as secure machine registration while removing the retired LaunchAgent, systemd, Windows Scheduled Task, and WSL Connector service lifecycle.

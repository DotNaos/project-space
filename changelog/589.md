---
bump: patch
---

# Restore the Pull Request Preview inventory

The trusted Preview hub can read its bounded, secret-free status registry from
the production bind mount again. Preview inventory and lifecycle actions no
longer fail closed because the isolated web container lacks the runner's host
group membership.

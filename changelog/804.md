---
bump: patch
---

# Restore Project Space browser startup

Project Space now uses the UI library release that loads syntax-highlighting
languages only after their browser runtime is ready, preventing a blank page on
fresh application loads.

---
bump: patch
---

# Finish reliable Codespaces runner startup

GitHub Codespaces now start their managed Codex service automatically, prepare
Project-managed worktrees correctly, and run issue tasks without repeated local
permission prompts. Startup also installs a current checksum-pinned runner
bundle and skips unnecessary Docker and Go checks. The Linux installer supports
container-owned connector supervision without requiring systemd, and future
signed updates accept the complete bundled Codex runtime.

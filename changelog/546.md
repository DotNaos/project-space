---
bump: patch
---

# Install the Codespaces-aware connector

New GitHub Codespaces runners now install the checksum-pinned connector release
that binds itself to its provider-managed execution environment automatically.

---
bump: minor
---

# Audit and safely reclaim local project storage

The Project CLI can now measure local checkout and worktree storage, identify
Project-managed worktrees that pass strict safety checks, and remove them only
after an explicit apply step. Main checkouts use additional recovery and remote
history checks before they can be removed.

---
bump: patch
---

# Complete Codespaces native dependency setup

GitHub Codespaces now install the pinned native build helper before project
dependencies, allowing the managed runner bootstrap to finish and reach its
web-based connector approval step.

## Canonical Runtime inspection

- Inspect Git status, diff summaries, worktrees, and declared development servers through the authenticated Workspace Runtime.
- Bind every request to one exact Environment, Workspace, generation, and live Runtime session with durable replay protection.
- Keep commands fixed and results privacy-safe without a Connector fallback, raw diffs, paths, credentials, or provider details.

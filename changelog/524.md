---
bump: minor
---

# Require a changelog and release bump for every pull request

Every new pull request now carries one immutable raw changelog file. Its
`patch`, `minor`, or `major` bump is consumed by the serial release queue, and
the Docs Unreleased Inbox keeps the raw entry visible until a published
version is assigned.

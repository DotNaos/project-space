---
bump: patch
---

# Retire legacy inspection aliases

Route legacy dev-server and worktree setup inspection requests to the canonical Runtime migration response. The core API no longer imports the Connector command hub for these paths.

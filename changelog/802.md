---
bump: patch
---

# Restore Preview GitHub TLS trust

Project Space web runtimes now include the standard CA trust store required for
trusted Preview GitHub API calls, avoiding false repository-permission failures
caused by missing certificate authorities.

---
bump: patch
---

# Bound VPS development sandboxes

Project Space now has a durable, fail-closed admission boundary for bounded VPS
development sandboxes. Reservations preserve Production capacity, cover every
configured resource dimension, record required isolation, and fence uncertain
cleanup until the same host generation proves the sandbox is absent.

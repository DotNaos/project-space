---
bump: patch
---

# Restore local Codex tasks in Chat

Chat now lists locally published Codex tasks from fresh online machines in
production, without duplicating them outside their host section. Task-list
rows also open correctly from friendly project URLs such as `project-space`.

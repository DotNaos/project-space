---
bump: patch
---

# Retire the permanent Connector

Project Space now uses canonical Environments and scoped Workspace Runtimes as
the only supported setup path. Permanent Connector installation, enrollment,
WebSocket registration, credentials, and legacy machine setup controls have
been removed. Existing legacy calls fail predictably and point to
`project environment bootstrap` instead of silently falling back.

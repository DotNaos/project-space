---
bump: patch
---

# Delete Connector runtime maintenance

Remove the internal Connector restart, update, stop, rate-limit, and operation-store services. Machine runtime status now reports the retired path as unsupported instead of constructing a Connector maintenance stack.

---
bump: minor
---

# Manage development servers through Project

Project now runs each configured development server in one owned tmux session,
keeps ports distinct across worktrees, and verifies the exact Tailscale route
before reporting the server as ready. The new status, list, logs, attach, stop,
and reconcile commands keep these servers usable after terminals disconnect,
while an explicit local-only flag remains available for exceptional debugging.

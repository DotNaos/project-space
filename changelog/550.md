---
bump: patch
---

# Install the corrected Codespaces runner

New GitHub Codespaces now install the released runner build that preserves
their exact provider name. The download is pinned and verified so production
runner connections use the intended fix reliably.
